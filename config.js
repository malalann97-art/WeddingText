// 将本文件复制为 config.js，并填入你的 Key。
// 出于安全考虑，请勿提交包含真实 Key 的 config.js。

window.DEEPSEEK_CONFIG = {
  apiKey: "", // 可留空，页面也支持手动输入
  model: "deepseek-chat",       // 可选默认模型
  temperature: 0.7               // 可选默认温度
};


